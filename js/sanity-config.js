/* Sanity.io connection settings for greatscopeinsurance.com.
   projectId/dataset are public read values (site fetches via Sanity's public CDN).
   Can be overridden at build time by scripts/build-sanity-config.js when
   SANITY_PROJECT_ID / SANITY_DATASET env vars are set. */
window.GREATSCOPE_SANITY = {
  projectId: '10dparpu',
  dataset: 'production'
};
